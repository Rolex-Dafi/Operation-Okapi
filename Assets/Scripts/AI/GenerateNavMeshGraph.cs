using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateNavMeshGraph : MonoBehaviour
{
    // TODO generate point graph in code during runtime - after pcg of levels is done

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
